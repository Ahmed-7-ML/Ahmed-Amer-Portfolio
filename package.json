{
  "name": "portfolio-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "([ -d \"portfolio-system/frontend\" ] && cd portfolio-system/frontend || true) && npm run build",
    "vercel-build": "([ -d \"portfolio-system/frontend\" ] && cd portfolio-system/frontend || true) && npm run build"
  }
}
